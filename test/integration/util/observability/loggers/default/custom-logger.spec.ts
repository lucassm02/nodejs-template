import { createLogger } from 'winston';

import { ELASTICSEARCH, LOGGER } from '@/util';
import { CustomLogger } from '@/util/observability/loggers/default/custom-logger';

describe('CustomerLogger Class', () => {
  // TODO: this is a real TODO
  it.todo('should test elastic apm integration connected');
});
